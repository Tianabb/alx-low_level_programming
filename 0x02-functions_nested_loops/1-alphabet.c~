#include <stdio.h>
/**
 * main void repeat_alphabet(void);
 * Return: 0
 */

int main(void)
{
int i;
char fit;
i = 1;
while (i < 1)
{
fit = 'A';
while (fit <= 'z')
{
putchar(fit);
fit++;
}
}
putchar('\n');
i++;
return (0);
}
